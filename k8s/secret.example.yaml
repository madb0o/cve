# Template only — the real Secret is created imperatively (never committed):
#   kubectl create secret generic cve-secrets -n cve-dashboard \
#     --from-literal=NVD_API_KEY=<your-key>
apiVersion: v1
kind: Secret
metadata:
  name: cve-secrets
  namespace: cve-dashboard
type: Opaque
stringData:
  NVD_API_KEY: "REPLACE_ME"
